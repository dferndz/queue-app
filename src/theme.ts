import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#bf5700",
    },
    secondary: {
      main: "#333f48",
    },
    success: {
      main: "#579d42",
    },
  },
});

export default theme;
